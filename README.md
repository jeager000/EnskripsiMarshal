# EnskripsiMarshal

Alat untuk men-enskripsi skrip kamu.
installisasi:
apt update && apt upgrade -y
apt install git -y
apt install python -y
git clone 

Menjalankan:
cd EnskripsiMarshal
python marshal.py
